import nav from './nav';
import { top, bottom } from './footer';
import makeButton from './button';
import { makeColorStyle } from './button-style';

console.log(nav(), top, bottom, makeButton('create'), makeColorStyle('cyan'));

/**
 * First Module: Webpack from Scratch
 *
 * using webpack for first time
 * Adding npm scripts for environment builds
 * setting up debugging
 * Coding your first Module
 * Adding Watch mode
 * ES Module Syntax
 * CommonJS Export
 * CommonJS Named Exports
 * Tree Shaking (dead code elimination)
 * Webpack Bunlde Walktrough
 * Add webpack configuration example
 */
